# 🐍 Python Basics Exercise: The `print()` Function

This exercise is designed to introduce you to the fundamental **`print()` function** in Python. The `print()` function is essential for displaying output to the user or for debugging your code.

---

## 🎯 The Goal

Your objective is to write a single line of Python code that uses the `print()` function to display a specific message to the console.

## 📝 Instructions

1.  Open the main Python file for this exercise (e.g., `main.py` or your assigned workspace).
2.  Use the `print()` function to output the following text exactly:

    **"Hello World !"**

### Example of the Target Code Structure:

```python
print("Your text goes here")